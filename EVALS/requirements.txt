anthropic>=0.40.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
deepeval>=0.21.0
# Pinned <1.50: newer streamlit's bundled ASGI server requires a starlette version that
# conflicts with fastapi (used elsewhere in this portfolio, e.g. AGENTS/LLMOPS), which pins
# starlette<0.42. streamlit==1.39.0 uses the older tornado-based server and avoids the clash.
streamlit==1.39.0
psycopg2-binary>=2.9.9
pandas>=2.2.0
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
